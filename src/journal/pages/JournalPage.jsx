import { Typography } from "@mui/material"

export const JournalPage = () => {
  return (
    <div>
      <Typography variant='h1'>Journal page</Typography> {/*Typography: hace referencia a la tipografia que voy a usar, variant hace referencia el elemento html h1  */}
    </div>
  )
}

